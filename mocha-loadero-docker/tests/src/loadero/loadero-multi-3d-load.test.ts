var expect = require('chai').expect;

import {createTestCombination, getDefaultCombination} from "~/loadero/TFactory";

describe('loadero-multi-3d-load',  () => {
    it("test1", async function () {
        expect(false).to.be.equal(true);
    });
});
